# Copy to .env and fill in real values (never commit .env)
JTAP_SECRET_KEY=change-me-to-a-long-random-string
# Optional: persistent data directory (DB + uploads), e.g. /data on Railway/Render/Fly
# DATA_DIR=.
